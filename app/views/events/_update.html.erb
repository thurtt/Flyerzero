<h1>Event successfully updated!</h1>
<script>
    var dragdropPartial = '<%= escape_javascript( render(:partial => "dragdrop") )%>';
    var eventId = '<%= event_id %>';
    $('#clone_event img').attr( 'src', '<%= photo %>' );
    submitOk = true;
</script>